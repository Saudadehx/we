<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.student_management_system.mapper.EnrollmentMapper">

    <resultMap id="EnrollmentResultMap" type="com.example.student_management_system.model.Enrollment">
        <id property="id" column="id"/>
        <result property="studentId" column="student_id"/>
        <result property="courseOfferingId" column="course_offering_id"/>
        <result property="score" column="score"/>
    </resultMap>

    <select id="findByStudentId" parameterType="long" resultMap="EnrollmentResultMap">
        SELECT id, student_id, course_offering_id, score FROM enrollments WHERE student_id = #{studentId}
    </select>

    <select id="findByCourseOfferingId" parameterType="long" resultMap="EnrollmentResultMap">
        SELECT id, student_id, course_offering_id, score FROM enrollments WHERE course_offering_id = #{courseOfferingId}
    </select>

    <insert id="insert" parameterType="com.example.student_management_system.model.Enrollment">
        INSERT INTO enrollments(student_id, course_offering_id, score)
        VALUES(#{studentId}, #{courseOfferingId}, #{score})
    </insert>

    <update id="updateScore">
        UPDATE enrollments SET score = #{score} WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM enrollments WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="long" resultMap="EnrollmentResultMap">
        SELECT id, student_id, course_offering_id, score FROM enrollments WHERE id = #{id}
    </select>

    <delete id="deleteByCourseOfferingId" parameterType="long">
        DELETE FROM enrollments WHERE course_offering_id = #{courseOfferingId}
    </delete>

    <select id="countByCourseOfferingId" parameterType="long" resultType="int">
        SELECT COUNT(*) FROM enrollments WHERE course_offering_id = #{courseOfferingId}
    </select>

</mapper>